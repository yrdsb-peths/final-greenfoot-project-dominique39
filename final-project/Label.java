import greenfoot.*;

public class Label extends Actor
{
    /**
     * Create a new Label with the specific image given
     *
     * @param path  the image path and file name
     */
    public Label(String path)
    {
        setImage(path);
    }
}